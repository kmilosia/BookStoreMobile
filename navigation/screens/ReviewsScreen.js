import { ScrollView, Text, View } from "react-native";

export default function ReviewsScreen () {
    return(
        <ScrollView>
            <View>
                <Text>Wszystkie recenzje</Text>
            </View>
        </ScrollView>
    )
}